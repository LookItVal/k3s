apiVersion: v1
kind: Service
metadata:
  name: nabu-core-service
  namespace: nabu
spec:
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 32000
  selector:
    app: nabu-core
  type: NodePort