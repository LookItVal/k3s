apiVersion: v1
kind: Service
metadata:
  name: nabu-redis-service
  namespace: nabu-beta
spec:
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
  selector:
    app: nabu-redis
  type: ClusterIP
